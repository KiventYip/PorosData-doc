# 文档正文格式指南

本指南用于统一正文表现层，不改变现有文档框架、导航和章节结构。

## 页首导语

- 每页在 H1 之后，或在承担页首介绍作用的首个 H2 下，放置 1 段简短导语。
- 导语控制在 1 到 3 句，回答“这页讲什么”和“为什么值得继续看”。
- 该段落使用 `lead` 样式类。

```md
PorosData 面向科研文献处理与结构化交付场景。
{: .lead}
```

## 段落节奏

- 每个主要章节在列表、表格或图示之前，先用 1 句短说明做过渡。
- 如果这句话主要承担阅读引导作用，使用 `section-intro` 样式类。
- 每段尽量只表达一个判断，避免一段里堆叠过多并列信息。

```md
以下页面适合先建立整体理解，再进入具体模块。
{: .section-intro}
```

## 强调规则

- 原则、关键判断、概念名使用粗体。
- 包名、字段名、目录名、命令、文件格式使用行内代码。
- 不要把整句或整条列表整体加粗。

## 列表与表格

- 列表默认一条只表达一个重点。
- 导航型列表、能力清单、快速链接可使用 `tight-list` 压缩密度。
- 表格以“易扫读”为优先，左列尽量短标签，右列承载解释。

```md
- [快速开始](docs/get_started/quickstart.md)
- [使用示例](docs/references/examples.md)
{: .tight-list}
```

## 提示框使用

- `tip`：推荐阅读路径、起步方式、使用建议。
- `note`：边界说明、术语解释、适用前提。
- `warning`：限制条件、风险点、容易误读的行为。
- 短页面里不要连续堆叠多个提示框。
