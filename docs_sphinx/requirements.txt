# Documentation build requirements for Read the Docs
# Security: Pin major versions to prevent breaking changes
# Performance: Use compatible version ranges for better caching

# Core Sphinx with security patches
sphinx>=5.3.0,<6.0.0

# MyST Parser for Markdown support (linkify disabled for security)
myst-parser>=1.0.0,<3.0.0

# Read the Docs theme
sphinx-rtd-theme>=1.3.0,<2.0.0

# Documentation enhancements
sphinx-copybutton>=0.5.2,<1.0.0
sphinx-design>=0.5.0,<1.0.0
sphinx-tabs>=3.4.1,<4.0.0

# Security: Hash verification (uncomment if needed)
# --hash=sha256:...
# --hash=sha256:...