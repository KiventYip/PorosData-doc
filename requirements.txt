# requirements.txt - PorosData documentation site dependencies
# Pin MkDocs 1.x to avoid 2.0 incompatibility with current Material themes.
mkdocs>=1.5.0,<2.0.0             # Core MkDocs framework (stay on 1.x)
mkdocs-material>=9.5.0,<10.0.0   # Material theme (compatible with MkDocs 1.x)

# Multi-language scaffolding (kept enabled for Material blog integration)
mkdocs-static-i18n>=1.2.0,<2.0.0 # Suffix-based i18n; required by hooks/material_blog_i18n_posts.py

# Markdown extensions used across the site (math, code blocks, tables, etc.)
pymdown-extensions>=10.7,<11.0.0

# Macros / dynamic content
mkdocs-macros-plugin>=1.0.0,<2.0.0

# Material plugins requiring extra runtime dependencies
pillow>=10.0.0                   # Image processing (required by the optimize plugin)
cairosvg>=2.7.0                  # SVG rendering (required by social cards)

# Git metadata plugins
mkdocs-git-revision-date-localized-plugin>=1.2.0  # Last-updated timestamps
mkdocs-git-committers-plugin-2>=2.0.0             # Per-page contributors

# RSS feed for the Material blog (compatible with mkdocs-static-i18n)
mkdocs-rss-plugin>=1.12.0,<2.0.0

# Privacy plugin runtime dependency
requests>=2.31.0                 # HTTP client used to mirror external resources
